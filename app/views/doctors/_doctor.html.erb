<li>
  <%= "Server: #{doctor.server}" %>
  | <%= doctor.name %>
  | <%= "Level: #{doctor.level}" %>
  | <%= "AV: #{doctor.av}" %>
  | <%= "User: #{User.find_by_id(doctor.user_id).name}" %>
  | <%= link_to "delete", doctor, :method => :delete, :confirm => "You sure?", :title => "Delete #{doctor.name}" %>
</li>
