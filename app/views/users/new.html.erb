<%= form_for(@user) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t(:title_user_new), :class => "btn primary" %>&nbsp;
    <button type="reset" class="btn" style="margin-right:30px;"><%= t(:button_cancel) %></button>
  </div>
<% end %>
