<h1><%= "#{@user.name} - #{t(:title_doctor_new)}" %></h1>
<%= form_for @doctor do |f| %>
  <%= render 'shared/doctor_fields', :f => f %>
  <div class="actions">
    <%= f.submit t(:button_doctor_new) %>
  </div>
<% end %>